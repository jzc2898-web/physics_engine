[project]
name = "cont-rn"
version = "0.1"
description = "2D rigid-body physics engine built from scratch"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["world", "viewer"]
